/* DataSeeder disabled.
   Previously added to seed data via JPA repositories, but it caused compilation
   issues in the build environment (Lombok getters/setters not available during
   compilation). Seeding will be done using SQL migration files and API calls.
*/

