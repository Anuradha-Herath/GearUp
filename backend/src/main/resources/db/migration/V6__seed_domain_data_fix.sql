-- V6__seed_domain_data_fix.sql
-- This migration is skipped as it references non-existent tables (customers, employees)
-- The current schema uses users table for both customers and employees
-- Fixes that were intended here are handled by other means

-- This file remains for Flyway compatibility but doesn't execute any SQL